node_modules
.DS_Store
.output
.vinxi
.wrangler
dist
.tanstack
*.local

# secrets
.dev.vars
.env
.env.*

# build
/build
/.cache

# large scraped recipe dumps (keep out of git; load via pnpm seed:recipes)
data/recipes/*.json

# generated d1 seed sql (regenerate via pnpm seed:recipes)
drizzle/seed/*.sql

# ro agent coordination state
.ro/

# generated autonomous-run completion reports
.completion-reports/

# agent git worktrees (isolated subagent checkouts; never part of the parent repo)
.claude/worktrees/
