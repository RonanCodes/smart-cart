node_modules
.DS_Store
.output
.vinxi
.wrangler
dist
.tanstack
*.local

# secrets
.dev.vars
.env
.env.*

# build
/build
/.cache

# large scraped recipe dumps (keep out of git; load via pnpm seed:recipes)
data/recipes/*.json
