## What & why

<!-- Brief description of the change. -->

## Promotion to prod (develop → main only)

Required before merging to **main** (prod). Feature PRs into `develop` can ignore this section.

- [ ] Verified this works on **dev.souso.app**
- [ ] Core flows still work on dev (onboarding → build week → cart → order)
- [ ] No new errors in Sentry / Workers Logs on dev after testing
